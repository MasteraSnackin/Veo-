/**
 * Test setup file
 * Runs before all tests to configure the testing environment
 * 
 * With globals: true in vitest.config, afterEach and vi are available globally
 */

import '@testing-library/jest-dom'
